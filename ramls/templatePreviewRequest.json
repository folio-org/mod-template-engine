{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Request for inline template preview",
  "type": "object",
  "properties": {
    "header": {
      "type": "string",
      "description": "Template for header"
    },
    "body": {
      "type": "string",
      "description": "Template for body"
    },
    "templateResolver": {
      "type": "string",
      "description": "Template engine name (defaults to 'mustache' when omitted)",
      "default": "mustache"
    },
    "context": {
      "type": "object",
      "description": "Context object",
      "$ref": "context.json"
    }
  },
  "additionalProperties": false
}
